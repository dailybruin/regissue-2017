<!DOCTYPE html>
<html class="no-js" lang="en-US">
  <head></head>
  <body>
    {% include "head.njk" %}
    <header>
      <h1>Registration Issue 2017</h1>
      <svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 100 100" preserveAspectRatio="none">
        <polygon fill="white" points="0,100 100,0 100,100"/>
      </svg>
    </header>
    <div class="row hide-for-small-only" id="section-covers">
      <div class="small-12 medium-3 columns">
        <a href="#news">
          <img class="news" src="img/cover-opinion.png">
        </a>
      </div>
      <div class="small-12 medium-3 columns opinion">
        <a href="#opinion">
          <img class="opinion" src="img/cover-opinion.png">
        </a>
      </div>
      <div class="small-12 medium-3 columns ae">
          <a href="#a&e">
            <img class="ae" src="img/cover-ae.png">
          </a>
      </div>
      <div class="small-12 medium-3 columns sports">
        <a href="#sports">
          <img class="sports" src="img/cover-opinion.png">
        </a>
      </div>
    </div>
      {% for section_name, section in sections %}
      <hr>
        <div class="section row" id="{{section_name}}">
          <h2 class="section-header">{{section_name}}</h2>
          <div class="section-cards row small-up-1 medium-up-3 large-up-4">
            {% for article in section.articles %}
              <div class="column article-card">
                <a href="{{article.link}}" target="_blank">
                  <div class="article-image-link" style="background-image:url('{{article.image}}');">

                  </div>
                </a>
                <div class="article-details">
                  <a href="{{article.link}}" target="_blank">
                    <h3 class="article-title">{{article.title}}</h3>
                    <span>By {{article.author}}</span>
                    <p>{{article.description}}</p>
                  </a>
                </div>
              </div>
            {% endfor %}
          </div>
          <!-- <ul class="article-slider">
            {% for article in section.articles %}
              <li class="article-thumbnail">
                <div class="article-details">
                  <a href="{{article.link}}">
                    <span>BY {{article.author}}</span>
                    <p>{{article.description}}</p>
                    <h5>{{article.title}}</h5>
                  </a>
                </div>
                <a href="{{article.link}}" target="_blank">
                  <div class="article-image-link" style="background-image:url('{{article.image}}');">

                  </div>
                </a>

              <a href="{{article.link}}">
                <img class="article-image" src="{{article.image}}" alt="{{article.image_alt}}">
              </a>
              </li>
            {% endfor %}
          </ul> -->
        </div>
      {% endfor %}

    {% include "scripts.njk" %}
  </body>
</html>
